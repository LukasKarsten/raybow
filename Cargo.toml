[package]
name = "raybow"
version = "0.1.0"
edition = "2021"

[dependencies]
rapid-qoi = "0.6.1"
rayon = "1.6.0"
rand = "0.8.5"
clap = { version = "4.0.29", features = ["derive"] }
indicatif = { version = "0.17.2", features = ["rayon"] }

[profile.release]
lto = "fat"
codegen-units = 1
